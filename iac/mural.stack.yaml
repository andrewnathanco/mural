version: "3"
services:
  mural-beta:
    image: andyrewco/mural:$VERSION
    env_file:
      - ".env"
    environment:
      - DATABASE_FILE=/go/src/app/mural.db
      - VERSION=$VERSION
    volumes:
      - ./mural.db:/go/src/app/mural.db
    ports:
      - $PORT:1323
